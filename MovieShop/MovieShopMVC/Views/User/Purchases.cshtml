@model ApplicationCore.Models.UserPurchaseModel



<div class="rounded">
    <div class="container-fluid">
        <div class="row">
            @foreach(var movie in @Model.Purchases)
            {
                <div class="col-6 col-xl-2 col-sm-4 col-lg-3 ">
                   <div class="card mb-4">
@*                       <a asp-action="Details" asp-controller="Movies" asp-route-id="@Model.UserId">
*@                           <img src="@movie.PosterUrl" alt="@movie.MovieTitle" class="img-fluid">
        @*               </a>*@
                    </div>
                </div>
            }
        </div>
    </div>
</div>